spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
    # Указывается для использования корректной работы jpa-методов с бд
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect